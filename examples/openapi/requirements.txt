# Core dependencies (already in main package)
# monic-framework

# Optional: For FastAPI integration example
fastapi>=0.100.0
uvicorn[standard]>=0.22.0

# Optional: For advanced examples
pydantic>=2.0.0
